---
import BaseLayout from '../layouts/BaseLayout.astro';
import Card from '../components/Card.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
    .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
    .slice(0, 3);

const projects = (await getCollection('projects'))
    .slice(0, 2);
---

<BaseLayout title={SITE_TITLE} description={SITE_DESCRIPTION}>
    <!-- Hero Section -->
	<section class="relative py-20 sm:py-28 text-center overflow-hidden">
        <!-- Background Decor -->
        <div class="absolute inset-0 pointer-events-none -z-10">
            <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[800px] bg-gradient-to-b from-indigo-50/50 to-transparent dark:from-indigo-950/20 dark:to-transparent rounded-full blur-3xl opacity-60"></div>
            <div class="absolute top-20 left-1/4 w-72 h-72 bg-primary-400/10 rounded-full blur-3xl animate-blob"></div>
            <div class="absolute top-20 right-1/4 w-72 h-72 bg-indigo-400/10 rounded-full blur-3xl animate-blob animation-delay-2000"></div>
        </div>

		<h1 class="text-5xl sm:text-7xl font-extrabold tracking-tight text-slate-900 dark:text-white mb-8 animate-fade-in-up">
			你好，我是<span class="text-transparent bg-clip-text bg-gradient-to-r from-primary-600 via-indigo-600 to-primary-600 bg-[length:200%_auto] animate-gradient">林景彪</span>
		</h1>
		<p class="text-xl sm:text-2xl text-slate-600 dark:text-slate-300 max-w-3xl mx-auto mb-12 leading-relaxed animate-fade-in-up animation-delay-200">
			<span class="block mb-2 font-medium text-slate-800 dark:text-slate-100">全栈开发者 · 技术探索者 · 终身学习者</span>
			<span class="text-base sm:text-lg text-slate-500 dark:text-slate-400">偏好可维护的工程实践与扎实的性能优化，专注 Java 生态与现代前端技术。</span>
		</p>
		<div class="flex flex-wrap justify-center gap-4 animate-fade-in-up animation-delay-400">
			<a
				href="/about"
				class="group relative inline-flex items-center justify-center px-8 py-3.5 bg-slate-900 dark:bg-white text-white dark:text-slate-900 rounded-full font-bold transition-all hover:scale-105 hover:shadow-xl hover:shadow-slate-500/20 active:scale-95"
			>
                <span>查看简历</span>
                <svg class="w-4 h-4 ml-2 transition-transform group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" /></svg>
			</a>
			<a
				href="/projects"
				class="group inline-flex items-center justify-center px-8 py-3.5 bg-white dark:bg-slate-900 text-slate-900 dark:text-white border border-slate-200 dark:border-slate-800 rounded-full font-bold hover:bg-slate-50 dark:hover:bg-slate-800 transition-all hover:scale-105 hover:border-slate-300 dark:hover:border-slate-700 active:scale-95"
			>
                <span class="mr-2">🚀</span>
				项目作品
			</a>
			<a
				href="/honors"
				class="group inline-flex items-center justify-center px-8 py-3.5 bg-white dark:bg-slate-900 text-slate-900 dark:text-white border border-slate-200 dark:border-slate-800 rounded-full font-bold hover:bg-slate-50 dark:hover:bg-slate-800 transition-all hover:scale-105 hover:border-slate-300 dark:hover:border-slate-700 active:scale-95"
			>
                <span class="mr-2">🏆</span>
				大学荣誉
			</a>
		</div>
	</section>

	<!-- Tech Stack -->
	<section class="py-12 mb-16 border-y border-slate-100 dark:border-slate-800/50 bg-slate-50/30 dark:bg-slate-900/10">
		<div class="max-w-6xl mx-auto px-4">
            <div class="text-center mb-10">
			    <h2 class="text-sm font-bold text-slate-400 dark:text-slate-500 uppercase tracking-[0.2em]">技术栈 & 工具库</h2>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- Backend -->
                <div class="space-y-4">
                    <h3 class="text-center md:text-left text-sm font-semibold text-slate-900 dark:text-white mb-4 flex items-center justify-center md:justify-start gap-2">
                        <svg class="w-5 h-5 text-indigo-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2m-2-4h.01M17 16h.01" /></svg>
                        后端开发
                    </h3>
                    <div class="flex flex-wrap justify-center md:justify-start gap-2">
                        <span class="tech-badge bg-orange-50 text-orange-700 border-orange-100">Java</span>
                        <span class="tech-badge bg-green-50 text-green-700 border-green-100">Spring Boot</span>
                        <span class="tech-badge bg-blue-50 text-blue-700 border-blue-100">MySQL</span>
                        <span class="tech-badge bg-slate-50 text-slate-700 border-slate-100">MyBatis-Plus</span>
                        <span class="tech-badge bg-red-50 text-red-700 border-red-100">Redis</span>
                    </div>
                </div>

                <!-- Frontend -->
                <div class="space-y-4">
                    <h3 class="text-center md:text-left text-sm font-semibold text-slate-900 dark:text-white mb-4 flex items-center justify-center md:justify-start gap-2">
                        <svg class="w-5 h-5 text-pink-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" /></svg>
                        前端技术
                    </h3>
                    <div class="flex flex-wrap justify-center md:justify-start gap-2">
                        <span class="tech-badge bg-emerald-50 text-emerald-700 border-emerald-100">Vue.js</span>
                        <span class="tech-badge bg-sky-50 text-sky-700 border-sky-100">Tailwind CSS</span>
                        <span class="tech-badge bg-purple-50 text-purple-700 border-purple-100">Astro</span>
                        <span class="tech-badge bg-green-50 text-green-700 border-green-100">Uni-App</span>
                        <span class="tech-badge bg-yellow-50 text-yellow-700 border-yellow-100">JavaScript</span>
                    </div>
                </div>

                <!-- DevOps & Tools -->
                <div class="space-y-4">
                    <h3 class="text-center md:text-left text-sm font-semibold text-slate-900 dark:text-white mb-4 flex items-center justify-center md:justify-start gap-2">
                        <svg class="w-5 h-5 text-cyan-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" /><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" /></svg>
                        工具 & 运维
                    </h3>
                    <div class="flex flex-wrap justify-center md:justify-start gap-2">
                        <span class="tech-badge bg-blue-50 text-blue-700 border-blue-100">Docker</span>
                        <span class="tech-badge bg-orange-50 text-orange-700 border-orange-100">Git</span>
                        <span class="tech-badge bg-slate-50 text-slate-700 border-slate-100">Nginx</span>
                        <span class="tech-badge bg-indigo-50 text-indigo-700 border-indigo-100">Linux</span>
                        <span class="tech-badge bg-sky-50 text-sky-700 border-sky-100">VS Code</span>
                    </div>
                </div>
            </div>
		</div>
	</section>


    <!-- Latest Posts -->
    <section class="py-10 sm:py-12">
        <div class="flex justify-between items-center mb-8">
            <h2 class="text-2xl sm:text-3xl font-bold tracking-tight text-slate-900 dark:text-white">最新文章</h2>
            <a href="/blog" class="text-primary-700 dark:text-primary-300 font-medium hover:underline underline-offset-4">查看更多 &rarr;</a>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {posts.map((post) => (
                <Card post={post} />
            ))}
        </div>
    </section>
    
    <!-- Projects Preview -->
    <section class="py-10 sm:py-12 border-t border-slate-200/60 dark:border-slate-800/60">
        <div class="flex justify-between items-center mb-8">
            <h2 class="text-2xl sm:text-3xl font-bold tracking-tight text-slate-900 dark:text-white">精选项目</h2>
            <a href="/projects" class="text-primary-700 dark:text-primary-300 font-medium hover:underline underline-offset-4">查看更多 &rarr;</a>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            {projects.map((project) => (
                <Card post={project} />
            ))}
        </div>
    </section>
    <style>
        @keyframes blob {
            0% { transform: translate(0px, 0px) scale(1); }
            33% { transform: translate(30px, -50px) scale(1.1); }
            66% { transform: translate(-20px, 20px) scale(0.9); }
            100% { transform: translate(0px, 0px) scale(1); }
        }
        .animate-blob {
            animation: blob 7s infinite;
        }
        .animation-delay-2000 {
            animation-delay: 2s;
        }
        .animation-delay-4000 {
            animation-delay: 4s;
        }
        .animation-delay-200 {
            animation-delay: 0.2s;
        }
        .animation-delay-400 {
            animation-delay: 0.4s;
        }

        @keyframes fade-in-up {
            0% { opacity: 0; transform: translateY(20px); }
            100% { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in-up {
            animation: fade-in-up 0.8s ease-out forwards;
            opacity: 0; /* Start hidden */
        }

        @keyframes gradient-x {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }
        .animate-gradient {
            animation: gradient-x 6s ease infinite;
        }

        .tech-badge {
            display: inline-flex;
            align-items: center;
            padding: 0.25rem 0.75rem;
            border-radius: 0.375rem;
            font-size: 0.75rem;
            font-weight: 700;
            border-width: 1px;
            transition: transform 0.3s;
            cursor: default;
        }
        .tech-badge:hover {
            transform: translateY(-2px);
        }
    </style>
</BaseLayout>
